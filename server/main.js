import { Meteor } from 'meteor/meteor'
import { Games, Cards } from '../imports/api/collections'
//
Meteor.startup(() => {
  // Cards.insert({type: 'white', set: 'basic', text: "Zeus's sexual appetites."})
});
